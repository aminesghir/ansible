server {
  listen 80;
  listen [::]:80;
  server_name www.{{ domain }};
  return 301 $scheme://{{ domain }}$request_uri;
}

server {
  listen 80;
  listen [::]:80;
  server_name {{ domain }};
  root /var/www/{{ domain }};

  index index.html;
}
